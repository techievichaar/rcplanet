<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Your Order Has Shipped - #<%= orderNumber %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        .logo {
            max-width: 200px;
            margin-bottom: 20px;
        }
        .shipping-info {
            background-color: #f9f9f9;
            padding: 20px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        .tracking {
            background-color: #e9f7fe;
            padding: 15px;
            border-radius: 5px;
            margin-top: 20px;
        }
        .button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 20px;
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            font-size: 12px;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="header">
        <img src="<%= process.env.LOGO_URL %>" alt="RCPlanet Logo" class="logo">
        <h1>Your Order Has Shipped!</h1>
    </div>

    <div class="shipping-info">
        <h2>Order #<%= orderNumber %></h2>
        <p>Great news! Your order has been shipped and is on its way to you.</p>

        <div class="tracking">
            <h3>Tracking Information</h3>
            <p>
                Carrier: <strong><%= tracking.carrier %></strong><br>
                Tracking Number: <strong><%= tracking.number %></strong>
            </p>
            <% if (tracking.url) { %>
                <p>You can track your package using the following link:</p>
                <a href="<%= tracking.url %>" class="button">Track Package</a>
            <% } %>
        </div>

        <% if (estimatedDelivery) { %>
            <p style="margin-top: 20px;">
                Estimated Delivery Date: <strong><%= estimatedDelivery.toLocaleDateString() %></strong>
            </p>
        <% } %>
    </div>

    <div style="text-align: center;">
        <a href="<%= trackingUrl %>" class="button">View Order Details</a>
    </div>

    <div class="footer">
        <p>If you have any questions about your order, please contact our customer service team.</p>
        <p>© <%= new Date().getFullYear() %> RCPlanet. All rights reserved.</p>
    </div>
</body>
</html> 